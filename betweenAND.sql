
-- Gets the actor_id's between 10 and 50
select * from sakila.actor where actor_id between 10 and 50;

-- Gets the values of first names between a and c so a and b first names show
select * from sakila.actor where first_name between 'a' and 'c';